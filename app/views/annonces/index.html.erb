<p id="notice"><%= notice %></p>

<h1>Listing Annonces</h1>

<table>
  <thead>
    <tr>
      <th>Titre</th>
      <th>Description</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @annonces.each do |annonce| %>
      <tr>
        <td><%= annonce.name %></td>
        <td><%= annonce.description %></td>
        <td><%= annonce.user.email if annonce.user %></td>
        <td><%= link_to 'Show', annonce %></td>
        <% if annonce.user == current_user %>
          <td><%= link_to 'Edit', edit_annonce_path(annonce) %></td>
          <td><%= link_to 'Destroy', annonce, method: :delete, data: { confirm: 'Are you sure?' } %>
          </td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<% if user_signed_in? %>
  <%= link_to 'Nouvelle annonce', new_annonce_path %>
<% end %>

