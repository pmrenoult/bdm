<div class="jumbotron">
  <div class="container">
		<h1>Bienvenue</h1>
		<% if user_signed_in? %>
		          # do something
		     <% else %>
		     		<p>Donnons une seconde vie à tous les matériaux de construction</p>
		       <p>
		         <%= link_to "Connexion", new_user_session_path, class: "btn btn-default btn-lg" %>
		         <%= link_to "S'enregistrer", new_user_registration_path, class: "btn btn-primary btn-lg" %>
		       </p>
		  <% end %> 
  </div>
</div>